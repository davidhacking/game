[project]
name = "win-ctrl"
version = "0.1.0"
description = "Windows 命令行控制工具（通过 SSH）"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "paramiko>=3.0",
]

[project.scripts]
win-ctrl = "win_ctrl:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
